test:
  name: certificate-benchmark-test
  description: Performance evaluation of issuing and verifying digital certificates
  workers:
    type: local
    number: 4   # تقليل العمال لتكون أقرب لاختبارات أكاديمية قياسية

  rounds:

    # 🔹 Issue – 100 TPS
    - label: Issue-100TPS
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/issueCertificate.js
        arguments:
          contractId: basic

    # 🔹 Issue – 200 TPS
    - label: Issue-200TPS
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 200
      workload:
        module: workload/issueCertificate.js
        arguments:
          contractId: basic

    # 🔹 Verify – 100 TPS
    - label: Verify-100TPS
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/verifyCertificate.js
        arguments:
          contractId: basic

    # 🔹 Verify – 200 TPS
    - label: Verify-200TPS
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 200
      workload:
        module: workload/verifyCertificate.js
        arguments:
          contractId: basic
